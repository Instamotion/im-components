/* eslint-env jest */
// import React from 'react';
// import { mount } from 'enzyme';
// import Validation from '../src';

describe('Validation', () => {
  it('write it actually', () => {
    expect(1).toBe(1);
  });
});
