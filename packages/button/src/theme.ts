import theme from '@im-ui/theme';

export default {
  primary: {
    borderColor: theme.color.downy,
    color: theme.color.oil,
    background: theme.color.downy
  },
  secondary: {
    borderColor: theme.color.downy,
    color: theme.color.downy,
    background: '#fff'
  }
};
